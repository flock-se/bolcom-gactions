1. test voor elke state/intent. 

2. Bepaal eerst de state waar we inzitten, process dan de intent voor die state.

* helpIntent (you can confirm this book, ask more details,) geef de opties die je kan doen vanuit de state

* availability (when would it be delivered, what is the availability). Match met enum van NL strings en vertaal naar engels.

* API call for cheapest price

* releasedate (proberen uit summary te parsen)


Nog niet:
// TODO: Add intents for requesting the book type, language, year